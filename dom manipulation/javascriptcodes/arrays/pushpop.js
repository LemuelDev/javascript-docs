// exercise 8
// Create an array of numbers and use the .push() method to add a new number to the end of the array. Then use the .pop() method to remove the last number from the array.

let favnumbers = [13, 17, 22, 8, 30, 35, 24, 1]

favnumbers.pop() //remove the last number
console.log(favnumbers)

favnumbers.push(32) // creating a new one
console.log(favnumbers)

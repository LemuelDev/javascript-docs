let even = [1, 3, 5, 10, 4, 9, 6, 13]
let evennums = odd.filter(num => num % 2 === 0 )
console.log(evennums)

// exercise 4
// Create an array of numbers and use the .filter() method to create a new array that only includes even numbers.
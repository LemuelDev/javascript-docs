// exercise 6
// Create an array of your favorite colors and console.log the first and last colors in the array.

let colors = ["Black", "White", "Gray", "Brown"]
console.log(colors[0] + " and " + colors[colors.length -1])
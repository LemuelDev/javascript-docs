// exercise 9
// Create an array of numbers and use the .slice() method to create a new array with only the first two numbers.

let num = [ 13 , 17, 22, 8]
let numslice = num.slice(0, 2)
console.log(numslice)